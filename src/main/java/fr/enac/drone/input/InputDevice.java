package fr.enac.drone.input;

/**
 * Physical input source most recently used to control the simulator.
 */
public enum InputDevice {
    /** Keyboard controls are currently the active input source. */
    KEYBOARD,
    /** Game controller controls are currently the active input source. */
    JOYSTICK
}
