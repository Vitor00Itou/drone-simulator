package fr.enac.drone.view.settings;

/**
 * Source selected when applying a world change from the settings drawer.
 */
public enum SettingsWorldSource {
    /** Load an existing saved world configuration. */
    PREDEFINED,
    /** Generate a random world from a density preset. */
    RANDOM
}
